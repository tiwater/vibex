/**
 * @vibex/core - Shared Types and Interfaces
 *
 * This package contains ONLY types and interfaces shared across all VibeX packages.
 * It has no runtime code and no implementations.
 *
 * All packages can safely depend on @vibex/core without creating cycles.
 */

export * from "./types";
